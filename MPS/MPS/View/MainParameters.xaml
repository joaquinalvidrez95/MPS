<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MPS"
             Title="Principal"
             x:Class="MPS.MainParameters">
    <ContentPage.Content>
        <StackLayout>
            <Button
                x:Name="buttonMainShowDate"
                Text="Actualizar fecha y hora"      
                HorizontalOptions="Fill"
                VerticalOptions="Fill"              
                />
          
            <Image Source="ic_access_time_black_48dp.png">
                <Image.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding DateTimeCommand}"/>
                </Image.GestureRecognizers>
            </Image>
            <Slider Minimum="0"
                    x:Name="slider"
                    Maximum="9"                          
                    ValueChanged="slider_ValueChanged"/>

            <Label x:Name="labelParametersBrightness"
                Text="{Binding CurrentDateTime}"
                   HorizontalOptions="CenterAndExpand"      
                   />


        </StackLayout>
    </ContentPage.Content>
</ContentPage>