<?xml version="1.0" encoding="utf-8" ?>
<pages:PopupPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
             xmlns:behaviour="clr-namespace:MPS.Behaviour;assembly=MPS"
             x:Class="MPS.MessagePopup">
   
    <!--<pages:PopupPage.Animation>
        <animations:ScaleAnimation 
            PositionIn="Center"
            PositionOut="Center"
            ScaleIn="1.2"
            ScaleOut="0.8"
            DurationIn="400"
            DurationOut="300"
            EasingIn="SinOut"
            EasingOut="SinIn"
            HasBackgroundAnimation="True"/>
    </pages:PopupPage.Animation>-->

    <pages:PopupPage.Resources>
        <ResourceDictionary>
            <Style TargetType="Button">
                <Setter Property="BackgroundColor"
                        Value="{StaticResource ButtonColor}"/>
                <Setter Property="TextColor"
                        Value="{StaticResource ColorButtonText}"></Setter>
            </Style>
        </ResourceDictionary>
    </pages:PopupPage.Resources>
    <StackLayout VerticalOptions="CenterAndExpand"
                 HorizontalOptions="Center"
                 BackgroundColor="White"
                 Padding="10,0,10,0"
                 >
        <TableView Intent="Form"
                   HasUnevenRows="True"                  
                   HorizontalOptions="Center"
                   VerticalOptions="Center">

            <TableSection Title="{Binding PopupTitle}">
                <EntryCell Label="Título"
                           Text="{Binding Title}"/>
                <ViewCell>
                    <ViewCell.View>
                        <StackLayout>
                            <Editor HeightRequest="{StaticResource EditorHeight}"
                                    Text="{Binding Text}">
                                <Editor.Behaviors>
                                    <behaviour:EditorLengthValidatorBehavior MaxLength="{StaticResource MaxMessageLength}"/>
                                </Editor.Behaviors>
                            </Editor>
                            <StackLayout Orientation="Horizontal">
                                <Button Text="{StaticResource ButtonAccept}"  
                                        HorizontalOptions="FillAndExpand"
                                        Command="{Binding DoneCommand}"/>
                                <Button Text="{StaticResource ButtonCancel}"           
                                        HorizontalOptions="FillAndExpand"
                                        Command="{Binding CancelCommand}"/>
                            </StackLayout>
                        </StackLayout>
                    </ViewCell.View>
                </ViewCell>
            </TableSection>
        </TableView>
    </StackLayout>

</pages:PopupPage>